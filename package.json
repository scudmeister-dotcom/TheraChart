{
  "name": "therachart",
  "version": "1.0.0",
  "description": "TheraChart EMR — Node clinic server (no deps by default; optional Postgres for durable Cloud Run storage)",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node test/db.test.js && node test/files.test.js && node test/auth.test.js && node test/store.test.js && node test/refine.test.js && node test/insights.test.js && node test/parser.test.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": "22.x"
  }
}
